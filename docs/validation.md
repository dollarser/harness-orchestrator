# 验证边界

0.6.0 将验证对象改为接入和配置管理；旧版编排测试已随被删除实现移除。

## 自动检查

- `npm run check`：构建、Node 测试、文档链接。
- `npm run test:ui`：浏览器配置页交互与窄屏布局。
- `npm pack --dry-run`：打包内容检查。

测试覆盖：嵌套工具启用、保留 persona/注释/JS 标签、缺失工具追加、禁用分组、动态条件、幂等、版本冲突、只读预设、外部修改保护、精确还原、提示词开关持久化、安装失败和凭据输出不泄漏、并发操作拒绝。

浏览器测试覆盖：状态分离、显式预设选择、工具启用、提示词持久化、只读预设、冲突、连接失败、未知登录状态及移动布局。

真实 Host 验证和模型调用结果必须单独记录；模拟安装命令测试不能代表真实包安装，工具可见性不能代表 Codex/Claude 模型调用通过。

## 2026-09-16 本地验证

- 15 项 Node/Host 测试、8 项 Playwright 测试通过。
- `node scripts/check-install.mjs` 在临时 Profile 中真实执行 pnpm，分别安装 Codex/Claude Code 后端、启用原生工具、精确还原预设；临时 Profile 已清理。
- 当前 DSH 源码 Host（0.1.6-alpha.1）实际加载打包插件，配置页通过宿主认证通道读取后端注册和运行实例工具状态。
- 自研搜索模式中两个原生工具可见；两项 CLI 登录检测均返回已登录。分工指引开关通过页面成功保存。
- 通过临时只读探针检查真实 system prompt assembly：独立指引片段存在、原 persona 保留、旧 `/smart-dev` 命令不存在、两个原生工具可见；探针随后移除，恢复普通启动。
- 未认证请求返回 HTTP 401。
- 未发送真实模型任务；不将安装、登录或工具可见性描述为多 Agent 任务端到端成功。

真实联调使用 Connection 的精确 Fetch 路由，避免争用 Host 独占的 `/api` interceptor，也不依赖独立 RPC channel 的服务注入行为。
